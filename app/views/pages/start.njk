{% extends "layouts/main.njk" %}
{% from  "hmrc/components/timeout-dialog/macro.njk" import hmrcTimeoutDialog %}

{% block casaPageTitle %}
  {{ t('start:pageTitle') }}
{% endblock %}

{% block content %}
  <h1 class="govuk-heading-xl">
    {{ t('start:title') }}
  </h1>

  {{ hmrcTimeoutDialog ({
    timeout: 70,
    countdown: 68,
    keepAliveUrl: "?continue=true",
    signOutUrl: "?lang=English",
    language: "en"
  }) }}


  {# <script nonce="{{ cspNonce }}" defer>
    function getCookie(name) {
      const value = `; ${document.cookie}`;
      const parts = value.split(`; ${name}=`);
      if (parts.length === 2) 
        return parts
          .pop()
          .split(';')
          .shift();
      }
    
    console.log(getCookie('myappsessionid.t'));
    console.log('doc cookies: ', document.cookie);


  </script> #}
{% endblock %}
